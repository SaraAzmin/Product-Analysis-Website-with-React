import React from 'react';

const WhatIsSemanticTag = () => {
    return (
        <div className='mx-10 md:mx-20 mb-20'>
            <h4 className='text-2xl font-semibold'>What is Semantic Tag?</h4>
            <p className='md:px-20 text-justify my-5'>Semantic tag means meaningful tags. Which gives the idea about the content inside the tag. One can distinguish what the element will look like on the page by these tag. It makes the code more readable. HTML5 has added some semantic tags such as: header, nav, main, footber, article, aside etc.  </p>
        </div>
    );
};

export default WhatIsSemanticTag;